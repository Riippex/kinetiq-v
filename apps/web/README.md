# Kinetiq V Web

The browser client uses Next.js App Router and participates in the root npm workspace.

- Run locally from the repository root with `npm run dev:web`.
- Create a production build with `npm run build:web`.
- Keep database access and credentials in the Django backend; server-side browser code calls the GraphQL API.
